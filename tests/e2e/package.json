{
  "name": "pickerwheel-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Browser end-to-end tests for the customer wheel page, run against a live PickerWheel instance (docker-compose up).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
