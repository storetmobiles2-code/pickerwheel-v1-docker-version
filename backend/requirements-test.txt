# Test-only dependencies. Not installed in the production image -
# install with: pip install -r backend/requirements-test.txt
# (in addition to backend/requirements.txt)

pytest==7.4.4
pytest-order==1.2.0
